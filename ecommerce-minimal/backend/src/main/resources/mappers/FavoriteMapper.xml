<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ecommerce.mapper.FavoriteMapper">
  <insert id="insert">insert ignore into product_favorite(user_id,product_id) values(#{userId},#{productId})</insert>
  <delete id="delete">delete from product_favorite where user_id=#{userId} and product_id=#{productId}</delete>
  <select id="count" resultType="int">select count(*) from product_favorite where user_id=#{userId} and product_id=#{productId}</select>
  <select id="findByUserId" resultType="com.example.ecommerce.model.Product">select p.* from product_favorite f join product p on p.id=f.product_id where f.user_id=#{userId} order by f.id desc</select>
</mapper>

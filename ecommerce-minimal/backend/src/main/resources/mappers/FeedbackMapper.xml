<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ecommerce.mapper.FeedbackMapper">
  <select id="findAll" resultType="com.example.ecommerce.model.Feedback">select * from feedback order by created_at desc,id desc</select>
  <select id="findByUserId" resultType="com.example.ecommerce.model.Feedback">select * from feedback where user_id=#{userId} order by created_at desc,id desc</select>
  <insert id="insert" useGeneratedKeys="true" keyProperty="id">insert into feedback(user_id,content,reply,status,created_at) values(#{userId},#{content},#{reply},#{status},#{createdAt})</insert>
  <update id="reply">update feedback set reply=#{reply},status=#{status} where id=#{id}</update>
  <update id="markProcessed">update feedback set status='PROCESSED' where id=#{id}</update>
</mapper>

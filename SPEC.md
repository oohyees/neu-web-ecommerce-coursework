# Web 大作业满绩补强 Spec：非报告部分

## 0. 目标与范围

目标：围绕指导书采分表，在不重构主架构的前提下，补强现场逐项给分时可能不稳的功能点，确保老师按表检查时每个采分点都有明确入口、可执行流程、可见结果和测试证据。

项目现有架构为 Vue3/Vite/Element Plus/Pinia 前端、Spring Boot 单体后端、Spring Cloud Gateway/Nacos/OpenFeign 微服务、MySQL、Redis、Docker Compose，且已有单体与微服务两套运行方式。仓库 README 已列出单体、微服务、Docker、Nacos、Gateway、Redis、MailHog、验收脚本等结构和运行方式。([GitHub][1])

本 spec 不包含报告补写，因为报告默认按满处理。重点只处理：客户端功能、后台功能、进阶功能、拓展功能、演示稳定性。

指导书采分点包括基础 20 分、进阶技术 20 分、拓展创新 10 分，其中基础覆盖客户端商品/购物车/订单和后台管理员/数据/用户/商品/订单，进阶覆盖 Element Plus、Pinia、鉴权、界面、用户认证、首页、地址、规格/优惠券/促销、轮播、系统管理、导入导出、响应式、上传、分页，拓展覆盖 Redis、微服务、Docker。

---

# 1. 总体补强原则

## 1.1 不做高风险重构

禁止在临交前做这些事：

```text
1. 不重构完整 SKU 库存矩阵
2. 不改微服务为独立数据库
3. 不新增复杂分布式事务
4. 不新增 WebSocket 实时客服
5. 不重写鉴权框架
6. 不大改前端布局体系
```

原因：这些改动成本高、回归风险大，且指导书没有要求达到工业级复杂度。

## 1.2 优先补“现场可见性”

每个采分点必须满足四个条件：

```text
有入口：老师能在页面上找到
有动作：老师能点按钮/提交表单
有结果：页面或数据能变化
有证据：接口、日志、脚本或截图能证明
```

如果代码已有但入口不明显，优先补入口。
如果接口已有但页面没有闭环，优先补页面闭环。
如果页面已有但没有演示数据，优先补 seed/reset 数据。

---

# 2. 高优先级补强项

## 2.1 管理员角色与权限分配演示

### 当前问题

指导书中“角色管理（超级管理员 / 普通管理员，权限分配）”虽然属于可选，但目标满绩时应尽量覆盖。当前项目已有 `SUPER_ADMIN` 和 `ADMIN` 两级角色，但现场如果只展示管理员登录，老师可能认为“权限分配”不足。

### 目标

实现并演示：

```text
超级管理员：可进入管理员管理页，可新增/编辑/禁用管理员
普通管理员：不可进入管理员管理页，访问时提示权限不足或 403
```

### 补强要求

前端：

```text
1. 后台菜单中增加或确认已有“管理员管理”入口
2. SUPER_ADMIN 登录时显示该菜单
3. ADMIN 登录时隐藏该菜单
4. 如果 ADMIN 手动访问 /admin/admins，显示“无权限访问”
```

后端：

```text
1. 管理员管理接口要求 SUPER_ADMIN
2. 普通 ADMIN 请求时返回 403
3. 返回信息清晰，例如：权限不足，仅超级管理员可操作
```

演示数据：

```text
super admin：admin / admin123
普通管理员：manager / manager123
```

验收步骤：

```text
1. 使用 admin 登录后台
2. 进入管理员管理页
3. 新增或编辑普通管理员
4. 退出
5. 使用 manager 登录
6. 确认管理员管理菜单不可见
7. 手动访问管理员管理 URL，确认 403 或无权限页
```

验收标准：

```text
普通管理员确实不能管理管理员账号
超级管理员确实能管理管理员账号
```

优先级：P0。

---

## 2.2 商品规格选择闭环

### 当前问题

指导书要求商品规格选择，例如颜色、尺寸。当前项目有规格数据和规格接口，但满绩现场更看重：用户是否能选择规格、购物车是否显示规格、订单是否保留规格。

### 目标

不做完整 SKU 矩阵，只做课程级闭环：

```text
商品详情页选择规格
加入购物车时保存规格文本
购物车展示规格
结算页展示规格
订单明细展示规格
后台订单详情展示规格
```

### 补强要求

商品详情页：

```text
1. 显示规格组，例如颜色、尺寸
2. 每组必须选择一个选项
3. 未选择规格时点击加入购物车，提示“请选择规格”
4. 已选择规格后加入购物车，保存 specText
```

购物车页：

```text
商品名称下方显示规格：颜色：黑色；尺寸：L
```

结算页：

```text
订单确认商品清单中显示规格
```

我的订单/订单详情：

```text
订单明细中显示规格
```

后台订单详情：

```text
管理员查看订单时能看到规格
```

验收步骤：

```text
1. 进入一个带规格商品详情页
2. 选择颜色和尺寸
3. 加入购物车
4. 查看购物车规格文本
5. 去结算页查看规格文本
6. 提交订单
7. 在我的订单和后台订单详情查看规格文本
```

验收标准：

```text
规格选择不是只停留在详情页，而是进入购物车和订单快照
```

优先级：P0。

---

## 2.3 优惠券参与结算金额计算

### 当前问题

指导书要求优惠券功能、秒杀/促销活动。项目已有优惠券和促销，但如果只展示列表，不展示“优惠券影响订单金额”，老师可能认为业务闭环不完整。

### 目标

实现或确认：

```text
用户领取优惠券
结算页选择优惠券
订单总额显示优惠金额
提交订单后保存 discountAmount / coupon 信息
后台订单详情可见优惠金额
```

### 补强要求

用户端：

```text
1. 优惠券中心可以领取优惠券
2. 结算页展示可用优惠券
3. 选择优惠券后，总价实时变化
4. 显示：
   - 商品总额
   - 优惠金额
   - 实付金额
```

后端：

```text
1. 下单接口校验优惠券是否属于该用户
2. 校验优惠券是否未使用、未过期、满足门槛
3. 写入订单优惠金额
4. 标记优惠券为已使用
```

后台：

```text
订单详情显示优惠金额和实付金额
```

演示数据：

```text
准备一张可用优惠券：
满 100 减 10
未过期
alice 可领取或已领取
```

验收步骤：

```text
1. 用户进入优惠券页领取满减券
2. 加购超过门槛的商品
3. 进入结算页
4. 选择优惠券
5. 确认实付金额减少
6. 提交订单
7. 后台订单详情查看优惠金额
```

验收标准：

```text
优惠券不是静态展示，而是参与订单金额计算
```

优先级：P0。

---

## 2.4 促销/秒杀库存闭环

### 当前问题

促销有效期已经修过，但满绩要求不是仅“促销不为空”，而是要展示促销商品、促销价、促销库存或下单效果。

### 目标

实现或确认：

```text
首页展示促销商品
商品详情展示促销价
下单后促销库存减少
促销过期不会显示
```

### 补强要求

用户端：

```text
1. 首页有“秒杀/促销专区”
2. 促销商品卡片显示原价、促销价、剩余库存
3. 商品详情页显示促销信息
4. 结算页使用促销价
```

后端：

```text
1. 促销列表只返回有效期内活动
2. 下单时校验促销库存
3. 支付或提交订单后扣减促销库存
```

演示数据：

```text
促销商品：有效期至 2026-12-31 23:59:59
促销库存：大于 0
促销价：明显低于原价
```

验收步骤：

```text
1. 首页进入促销商品
2. 查看促销价和库存
3. 下单
4. 后台或数据库确认促销库存变化
```

验收标准：

```text
促销不是只展示 banner，而是能进入购买流程
```

优先级：P0。

---

## 2.5 分类排序现场可见

### 当前问题

指导书要求商品分类管理支持新增、修改、删除、排序分类。项目已有 `sort_order` 字段和排序查询，但如果页面没有明显排序操作，现场可能被扣小分。

### 目标

后台分类管理页可以直接修改排序值，并且刷新后排序变化。

### 补强要求

后台分类页：

```text
1. 表格显示排序值 sortOrder
2. 新增分类时可填写排序值
3. 编辑分类时可修改排序值
4. 列表按排序值升序展示
```

可选低成本增强：

```text
增加“上移”“下移”按钮
```

若不做上移/下移，至少必须能通过编辑排序值实现排序。

验收步骤：

```text
1. 进入后台分类管理
2. 修改某分类排序值为 1
3. 保存
4. 刷新列表
5. 确认该分类顺序变化
```

验收标准：

```text
老师能看到“排序分类”功能，而不是只听解释
```

优先级：P0。

---

## 2.6 商品导入/导出、订单导出现场可用

### 当前问题

指导书进阶有“导入导出”2 分，后台基础也要求订单导出。导入导出最容易现场因文件、浏览器下载、格式问题翻车。

### 目标

现场可完成：

```text
商品导出
商品导入
订单导出
```

### 补强要求

前端：

```text
1. 后台商品列表有“导出商品”按钮
2. 后台商品列表有“导入商品”按钮
3. 后台订单列表有“导出订单”按钮
4. 点击导出后浏览器下载文件
5. 导入后页面提示成功，并能看到新增商品
```

脚本/样例文件：

```text
docs/course/acceptance/sample-products-import.csv
```

样例文件只保留 1–2 个商品，避免污染演示数据。

建议 CSV 字段：

```csv
name,categoryId,price,stock,imageUrl,description
演示导入商品,1,99.00,100,/uploads/demo-import.png,用于演示商品批量导入
```

验收步骤：

```text
1. 后台商品列表点击导出
2. 确认下载文件
3. 选择 sample-products-import.csv 导入
4. 搜索“演示导入商品”
5. 后台订单列表点击导出
6. 确认下载文件
```

验收标准：

```text
浏览器下载正常
导入后列表中能看到新商品
```

优先级：P0。

---

# 3. 中优先级补强项

## 3.1 评价带图闭环

### 当前问题

指导书要求商品评价支持上传评价图片。当前模型看起来是单图字段，这不影响采分，但必须保证现场能上传并显示。

### 目标

```text
用户提交评价时上传图片
商品详情评价区显示该图片
后台评价管理显示该图片
管理员可删除违规评价
```

### 补强要求

用户端：

```text
1. 已完成订单可评价
2. 评价表单包含评分、文字、图片上传
3. 上传成功后显示缩略图
4. 提交后商品评价区显示图片
```

后台：

```text
1. 评价管理列表显示评价图片缩略图
2. 可删除评价
```

验收步骤：

```text
1. 使用已完成订单提交带图评价
2. 回到商品详情查看评价图片
3. 后台评价管理查看并删除该评价
```

优先级：P1。

---

## 3.2 图片上传入口覆盖至少两类

### 当前问题

统一上传接口存在，但老师看的是页面入口。指导书多个地方涉及图片上传：商品图片、评价图片、轮播图片、头像。

### 目标

至少两个入口现场可用：

```text
后台商品图片上传
用户评价图片上传
后台轮播图上传
用户头像上传
```

优先推荐：

```text
1. 后台商品图片上传
2. 后台轮播图上传
3. 用户评价图片上传
```

验收步骤：

```text
1. 后台新增/编辑商品，上传商品图片
2. 后台新增/编辑轮播图，上传轮播图片
3. 前台确认图片显示正常
```

优先级：P1。

---

## 3.3 首页二级分类与热门搜索词

### 当前问题

指导书进阶首页要求：轮播、一级/二级分类导航、热门商品、新品推荐、促销商品、搜索框、热门搜索词。首页内容越明显，越容易拿满首页小分。

### 目标

首页首屏或明显区域包含：

```text
轮播图
一级分类
二级分类
热门商品
新品推荐
促销商品
搜索框
热门搜索词
```

### 补强要求

首页搜索框下增加热门搜索词：

```text
手机
耳机
电脑
运动
家居
```

点击热门搜索词跳转商品列表并带关键词。

分类导航：

```text
一级分类显示在导航栏
鼠标悬停或点击一级分类后显示二级分类
点击二级分类进入商品列表
```

验收步骤：

```text
1. 首页查看轮播
2. 点击一级/二级分类
3. 点击热门搜索词
4. 查看热门、新品、促销区
```

优先级：P1。

---

## 3.4 前台响应式巡检

### 当前问题

已有后台 390px/768px 巡检，但满绩建议前台购物链路也要证明响应式。

### 目标

以下页面在 390px 和 768px 下无横向滚动、无按钮遮挡、核心流程可完成：

```text
首页
商品列表
商品详情
购物车
结算页
我的订单
```

### 补强要求

最低要求：

```text
1. 商品卡片在移动端单列或双列合理显示
2. 详情页图片和规格区域不溢出
3. 购物车商品行移动端可读
4. 结算页地址、商品、金额区域不溢出
5. 底部按钮可点击
```

验收步骤：

```text
浏览器 DevTools 设置 390px 宽度
走：首页 → 商品详情 → 加购 → 购物车 → 结算
```

优先级：P1。

---

## 3.5 客服咨询留言/回复闭环

### 当前问题

指导书允许“简单留言 / 在线聊天”。现有方向应按“简单留言”演示，不要声称实时聊天。

### 目标

```text
用户提交咨询
后台客服查看咨询
后台回复咨询
用户端看到回复
后台可标记已处理
```

验收步骤：

```text
1. 用户进入客服咨询页
2. 提交问题
3. 管理员后台进入客服管理
4. 回复问题并标记已处理
5. 用户端查看回复
```

优先级：P1。

---

## 3.6 系统公告、活动通知、反馈闭环

### 当前问题

指导书进阶要求系统公告、活动通知、用户反馈。代码已有相关页面和 Controller，但现场需要闭环。

### 目标

公告：

```text
后台发布公告
前台公告区展示
```

活动通知：

```text
后台发布活动通知
前台活动通知区展示
```

反馈：

```text
用户提交反馈
后台查看反馈
后台回复/标记已处理
```

验收步骤：

```text
1. 后台新增公告
2. 前台查看公告
3. 用户提交反馈
4. 后台回复反馈
5. 用户端看到处理状态
```

优先级：P1。

---

# 4. 拓展创新补强项

## 4.1 Redis 可见证据

### 当前问题

Redis 已使用，但老师逐项打分时必须能看见。Redis 不能只写在技术栈里。

### 目标

现场展示：

```text
Redis token/session key
验证码 key 或缓存 key
商品详情/首页缓存 key
清理 Redis 的 reset 脚本
```

### 补强要求

准备命令：

```bash
docker exec -it <redis-container> redis-cli keys '*'
docker exec -it <redis-container> redis-cli get '<token-key>'
docker exec -it <redis-container> redis-cli ttl '<cache-key>'
```

如果 key 是序列化对象，至少展示 key 名和 TTL。

页面演示：

```text
1. 登录用户
2. 查看 Redis 中 session/token key
3. 打开商品详情
4. 查看商品缓存 key
5. 执行 reset_demo_data.sh both
6. 确认 Redis 被清理
```

验收标准：

```text
老师能明确看到 Redis 不是摆设
```

优先级：P0。

---

## 4.2 微服务三件套证据

### 当前问题

微服务项 4 分，必须展示“拆分、独立部署、服务间通信”。目录存在不够。

### 目标

现场固定展示三件套：

```text
1. Nacos 服务注册列表
2. Gateway 统一入口访问 API
3. order-service 通过 Feign 调 catalog-service 扣减库存
```

### 补强要求

Nacos：

```text
打开 http://localhost:18098/nacos
展示 auth-service、catalog-service、order-service、admin-service、gateway
```

Gateway：

```bash
curl http://localhost:18090/api/products
```

Feign：

```text
运行 microservices_smoke_test.sh
查看日志中 order-service -> catalog-service 调用证据
下单后商品库存减少
```

验收步骤：

```text
1. docker compose -f docker-compose.microservices.yml ps
2. 打开 Nacos
3. 通过 Gateway 登录/访问商品
4. 创建订单
5. 展示库存扣减和 Feign 日志
```

优先级：P0。

---

## 4.3 Docker 兜底方案

### 当前问题

Docker 现场构建可能受网络影响。满绩需要确保 Docker 容器化可展示，而不是卡在拉镜像。

### 目标

准备三种启动路径：

```text
首选：docker compose up -d --no-build
次选：docker compose up -d --build
兜底：展示已运行容器和访问地址
```

### 补强要求

准备命令文件：

```bash
scripts/demo_start_monolith.sh
scripts/demo_start_microservices.sh
scripts/demo_check_services.sh
```

建议内容：

```bash
docker compose up -d --no-build
docker compose ps
curl -f http://localhost:18080/api/health || true
curl -f http://localhost:18081 || true
```

微服务：

```bash
docker compose -f docker-compose.microservices.yml up -d --no-build
docker compose -f docker-compose.microservices.yml ps
curl -f http://localhost:18090/api/products || true
```

验收标准：

```text
即使现场不能重新 build，也能展示容器化部署和服务访问
```

优先级：P0。

---

# 5. 课程基础技术证据补强

## 5.1 Servlet/JSP/Listener/Filter/JDBC 展示入口

### 当前问题

主业务是 Spring Boot + Vue，老师如果特别看课程基础技术，可能问 Servlet、JSP、Listener、Filter、JDBC 在哪里。

### 目标

保留并展示传统 Web 证据模块：

```text
Servlet
JSP
Listener
Filter
JDBC
```

README 已写 Course evidence: Servlet, JSP, Listener, Filter, JDBC，并提供 legacy status page 地址。([GitHub][1])

### 补强要求

现场入口：

```text
http://localhost:18080/legacy/status
```

页面内容建议显示：

```text
Legacy Servlet status
JSP rendered successfully
Listener initialized at: xxxx
Filter audit enabled
JDBC health check: OK
```

后台或 README 里列出源码路径：

```text
apps/api/src/main/java/.../legacy/LegacyStatusServlet.java
apps/api/src/main/java/.../legacy/LegacyAuditFilter.java
apps/api/src/main/java/.../legacy/LegacyAppListener.java
apps/api/src/main/java/.../legacy/LegacyJdbcDao.java
```

验收标准：

```text
老师能看到课程基础技术不是空写
```

优先级：P1。

---

# 6. 演示数据与验收脚本

## 6.1 reset_demo_data.sh 必须成为演示前固定动作

### 目标

每次现场演示前执行：

```bash
scripts/reset_demo_data.sh both
```

执行后必须保证：

```text
1. 无 QA/test/smoke/import 用户
2. 无 QA/test/smoke/import 商品
3. 促销有效期未过期
4. 首页促销不为空
5. 普通用户、普通管理员、超级管理员账号可用
6. Redis 已清理
```

## 6.2 新增 final_precheck 脚本

建议新增：

```bash
scripts/final_precheck.sh
```

内容：

```bash
#!/usr/bin/env bash
set -euo pipefail

npm --prefix apps/web run build
mvn test
./scripts/acceptance_check.sh
python3 scripts/acceptance_api_smoke.py
scripts/microservices_smoke_test.sh
scripts/reset_demo_data.sh both

echo "Final precheck passed. Demo data has been reset."
```

注意：如果 smoke 会污染演示数据，`reset_demo_data.sh both` 必须放最后。

优先级：P0。

---

# 7. 固定现场演示路线

## 7.1 用户端路线

```text
首页
→ 轮播/分类/热门搜索词/促销
→ 商品列表搜索与排序
→ 商品详情
→ 选择规格
→ 收藏
→ 查看/提交带图评价
→ 加入购物车
→ 修改数量/勾选/全选
→ 结算
→ 选择地址
→ 领取并选择优惠券
→ 提交订单
→ 模拟支付
→ 我的订单
→ 确认收货/退款/查看物流
```

## 7.2 后台路线

```text
管理员登录
→ 数据看板
→ 用户管理
→ 管理员角色权限演示
→ 分类管理，修改排序
→ 商品管理，新增/编辑/上传图片/上下架
→ 商品导入/导出
→ 订单管理，发货/退款/导出
→ 评价管理，查看带图评价/删除
→ 轮播管理，上传图片
→ 公告/活动通知
→ 反馈/客服处理
```

## 7.3 拓展路线

```text
Redis key
→ Docker Compose 容器列表
→ Nacos 服务注册
→ Gateway API
→ 微服务下单
→ Feign 日志
→ 库存扣减
→ 移动端响应式
→ legacy Servlet/JSP/JDBC 页面
```

---

# 8. 验收标准总表

| 模块        | 必须补强到的状态                             | 优先级 |
| --------- | ------------------------------------ | --- |
| 权限分配      | SUPER_ADMIN 与 ADMIN 权限差异现场可见         | P0  |
| 商品规格      | 详情选择、购物车、结算、订单均显示规格                  | P0  |
| 优惠券       | 领取、选择、抵扣、订单保存优惠金额                    | P0  |
| 促销/秒杀     | 展示促销价，参与购买，库存变化                      | P0  |
| 分类排序      | 后台可修改排序值并改变展示顺序                      | P0  |
| 导入导出      | 商品导入/导出、订单导出现场可用                     | P0  |
| Redis     | 展示 session/cache key 和清理脚本           | P0  |
| 微服务       | Nacos、Gateway、Feign、库存扣减证据           | P0  |
| Docker    | 使用本地镜像可稳定启动和展示容器                     | P0  |
| 评价图片      | 用户提交带图评价，前后台可见                       | P1  |
| 图片上传      | 商品/轮播/评价至少两类可上传                      | P1  |
| 首页        | 二级分类、热门搜索词、促销区明显                     | P1  |
| 响应式       | 前台核心购物链路 390px 无横向溢出                 | P1  |
| 客服        | 用户留言、后台回复、用户查看回复                     | P1  |
| 公告反馈      | 后台发布/处理，前台可见                         | P1  |
| Legacy 证据 | Servlet/JSP/Listener/Filter/JDBC 可访问 | P1  |

---

# 9. 不建议补的内容

以下内容不建议现在做：

```text
完整多维 SKU 矩阵
真实第三方支付接口
WebSocket 实时客服
独立数据库微服务
分布式事务
完整 CI/CD 平台
完整按钮级 RBAC
链路追踪平台
```

这些内容不是指导书硬要求，临近交付时投入产出比低，且容易破坏现有稳定性。

---

# 10. 最终交付前确认

执行：

```bash
scripts/final_precheck.sh
```

人工检查：

```text
1. 前台首页促销不为空
2. 商品详情规格可选
3. 购物车和订单显示规格
4. 优惠券能抵扣金额
5. 后台分类排序可见
6. 商品导入/导出可用
7. 订单导出可用
8. SUPER_ADMIN / ADMIN 权限差异明显
9. Redis key 可展示
10. Nacos 服务列表可展示
11. Gateway 能访问
12. Feign 调用证据可展示
13. Docker 容器列表正常
14. 移动端核心页面无明显错乱
15. legacy/status 可打开
```

最终判断标准：
**老师拿着采分表逐项检查时，不需要听复杂解释，每个点都能在 30 秒内看到页面、动作或证据。**

[1]: https://github.com/oohyees/neu-web-ecommerce-coursework "GitHub - oohyees/neu-web-ecommerce-coursework · GitHub"

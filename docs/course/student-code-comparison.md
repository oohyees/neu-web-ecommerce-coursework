# 同学电商项目代码对比静态巡检

巡检时间：2026-06-07

巡检范围：

- 本项目：`/Users/oohyees/Projects/NEU/sem6/web`
- 对比目录：`/Users/oohyees/Desktop/students`
- 依据文件：`docs/course/A0801051040-Web开发技术-实验指导书-2026.md`

说明：本报告基于静态扫描、README、`pom.xml`、`package.json`、Docker/SQL 文件、源文件命名和接口命名做对比。没有逐个启动运行，所以功能覆盖属于“代码与文档证据层面”的判断，不等价于实际可演示通过。

## 指导书核心评分点

课程要求可压缩为四类风险：

1. 基础 20 分：客户端商品、购物车、订单；后台管理员、数据、用户、商品、订单。
2. 进阶 20 分：Element Plus、Pinia、认证鉴权、界面、用户认证、首页、地址、规格/优惠券/秒杀、轮播、系统管理、导入导出、响应式、上传、分页。
3. 拓展 10 分：Redis、微服务、Docker。
4. 报告 10 分：实验目的、内容、环境、过程与分析、创新点、总结和提交规范。

## 目录到同学映射

| 同学/目录 | 代码目录 | 备注 |
| --- | --- | --- |
| 董耕赫 | `commerce-web-course-code` | zip 顶层也是该目录，轻量微服务示例风格明显。 |
| 董新畅 | `JavaWebExp` | zip 顶层是 `JavaWebExp`，README 记录从骨架逐轮演进。 |
| 代宇霆 | `20236917-代宇霆-软件2401-大作业-工程源码` | 多模块后端、双前端、图片资源较多。 |
| 周梦玲 | `e_commerce` | zip 顶层是 `e_commerce`，包含 RabbitMQ、WebSocket、OSS/SMTP/支付配置说明。 |
| 李博韬 | `web-dev-experiment` | zip 顶层是 `web-dev-experiment`，体积异常大，包含大量不应提交内容。 |
| 杨浩伟 | `20246152-杨浩伟-软件2403-大作业-工程压缩包` | 精简微服务，单前端聚合客户端和后台。 |
| 李旋 | `20231985-李旋-软件2303班-代码` | 包含 `.git`、`.npm-cache`、数据库数据文件，提交包体积严重超标。 |
| 李伟翔 | `20246176-李伟翔-软件2402-大作业-工程压缩包` | Nacos/OpenFeign 证据较强，但 zip 包含 `.git`、`.idea`、`.claude`。 |
| 郭振顺 | `20246047-郭振顺-软件2402-大作业-工程压缩包` | 多个后端服务，README 基本还是脚手架模板，缺 SQL 顶层证据。 |
| 张臻 | `20245738-张臻-软件2402-大作业-工程压缩包` | ShopSphere，服务拆分较完整，源码包很精简。 |

## 总览对比

| 项目 | 后端形态 | 前端形态 | 数据/部署 | 主要强项 | 主要风险 |
| --- | --- | --- | --- | --- | --- |
| 本项目 | 当前 Vue 前端 + Gateway 主验收，legacy 单体作传统 Web 对照证据 | shop/admin 双 Vue3 | MySQL、Redis、Docker、MailHog | 主线可验收、证据和报告矩阵完整，SMTP 验证真实 | 需要继续做浏览器人工巡检和截图新鲜度检查 |
| 董耕赫 | `api` 兼容层 + user/catalog/order 轻量拆分 | store/admin 双 Vue3 | Docker Compose、MySQL、Redis、Flyway | README 与评分点覆盖清楚，接口/页面命名完整 | 邮箱验证码 README 写固定 `123456`，微服务偏展示型 |
| 董新畅 | user/product/order/admin/marketing/gateway 多模块 | user/admin 双 Vue3 | 多 SQL 迁移、Dockerfile | 功能点非常接近指导书，代码量足 | 无顶层 compose；README 前半仍保留“未实现”历史叙述，需防文档自相矛盾 |
| 代宇霆 | auth/user/product/order/admin/gateway | user/admin 双 Vue3 | deploy compose、Redis、Excel | README 功能覆盖最满之一，上传/客服/调研/DeepSeek 亮点多 | AI/DeepSeek 属额外亮点但也可能引入无法演示配置风险 |
| 周梦玲 | gateway + auth/user/product/cart/order/chat | client/admin 双 Vue3 | Docker Compose、RabbitMQ、Redis、MySQL 外置 | 功能和创新点最多，含消息队列、WebSocket、6 库拆分 | 引入 RabbitMQ、OSS、支付宝等复杂依赖，课堂演示失败面更大 |
| 李博韬 | hmall 风格多服务，含 Nacos/OpenFeign | hmall-web/admin | Docker、Nacos、SQL、e2e | 微服务工程化最重，测试/文档多 | zip 215M 严重超过 50M，且包含大量缓存/生成物 |
| 杨浩伟 | gateway + user/product/order/common | 单 Vue3 聚合前后台 | Docker Compose、Redis、SQL | 源码包小，功能点覆盖面广 | 服务转发像手写 GatewayController，不一定是真 Spring Cloud Gateway；Java 23 环境要求偏高 |
| 李旋 | gateway + user/product/order/common-web | shop/admin 双 Vue3 | Docker、Nacos、SQL、JSP 演示 | 同时补了 Servlet/JSP/JDBC 课程传统点 | zip 532M 严重超标，包含 `.git`、`.npm-cache`、`.ibd/.sdi` 数据文件 |
| 李伟翔 | gateway + user/product/cart/order/coupon/admin | front/admin 双 Vue3 | Docker、Nacos/OpenFeign、Redis | 服务拆分细，优惠券、秒杀、操作日志等较全 | zip 包含 `.git`、`.idea`、`.claude`，提交规范风险 |
| 郭振顺 | user/product/order/trade 四后端 | 单 Vue3 | 多 compose、Redis | 控制器/页面命名显示基础功能覆盖较广 | README 仍是 Vue/Spring Initializr 模板；未发现 SQL 文件，数据库可复现性风险高 |
| 张臻 | gateway + auth/user/product/cart/order/payment | client/admin 双 Vue3 | Docker Compose、Nacos/OpenFeign、SQL | 支付服务单独拆分，README 清晰，包很小 | 前端 `.vue` 页面数量偏少，管理端/客户端是否完整需运行确认 |

## 技术栈共性

共同点：

- 前端基本都是 Vue 3 + Vite + Element Plus + Pinia + Axios。
- 后端基本都是 Spring Boot 3 + MyBatis/MyBatis-Plus + MySQL。
- 认证普遍使用 JWT，部分使用 Spring Security，部分使用 Shiro。
- 进阶项普遍覆盖 Redis、Docker、上传、Excel、分页、轮播、公告、反馈、优惠券/秒杀。

差异点：

- 微服务真实性差异大：有的是 Spring Cloud Gateway + Nacos + OpenFeign，有的是手写 GatewayController 或 `api` 聚合层展示。
- 数据库拆分差异大：周梦玲、李旋、张臻、李伟翔更偏多库/多服务；董耕赫和本项目更偏主库 + 渐进式拆分。
- 邮箱验证码实现差异大：有的是真 SMTP/邮件服务，有的 README 明示固定验证码或日志模式。
- 提交规范差异很大：源码包最容易丢分的不是功能，而是包含 `.git`、缓存、构建产物、数据库数据文件导致超 50M。

## 相似度与同源线索

静态扫描没有发现高置信的“大量完全相同源码哈希重叠”。但有几类同源/模板线索：

1. `commerce-web-course-code` 与董耕赫 zip 完全对应，属于同一个项目目录。
2. `JavaWebExp` 与董新畅 zip 对应。
3. `e_commerce` 与周梦玲 zip 对应。
4. `web-dev-experiment` 与李博韬 zip 对应。
5. 多数项目都使用课程电商通用命名：`ProductController`、`CartController`、`OrderController`、`AddressController`、`DashboardController`、`Banner`、`Coupon`、`Review`、`Favorite`，这属于需求天然重合，不能单独作为抄袭证据。
6. 李博韬项目明显带有黑马商城/hmall 命名体系；张臻是 ShopSphere 命名体系；这两者与本项目命名体系差异较明显。

## 对本项目的借鉴价值

最值得借鉴但不建议大改的点：

- 代宇霆：README 的功能概览写得很完整，适合借鉴报告表达方式，但不要引入 DeepSeek 等新依赖。
- 张臻：README 的“常用验证”段很适合作为答辩材料结构，验证命令清楚。
- 李伟翔/李旋：Nacos/OpenFeign、JSP/JDBC 证据比较显眼，可提醒我们在报告中强调已有 Nacos/Feign 证据和 legacy-web 课程传统点。
- 周梦玲：WebSocket 客服、RabbitMQ、OSS、支付宝属于展示亮点，但对当前项目是高风险扩展，不建议临近提交加入。
- 董耕赫：轻量微服务作为创新证据路径，与本项目“Vue 前端 + Gateway 主验收，legacy 作对照证据”的策略有可比性，可作为论证参考。

本项目当前相对优势：

- 已有 `docs/course/acceptance/checklist.md` 对照指导书评分点，不只是 README 自述。
- 已跑通过 `mvn test`、前端 build、monolith smoke、microservice smoke、gateway full smoke、submission zip。
- 提交包脚本已验证约 4.4M，明显优于多个同学的超大 zip。
- 邮箱验证码是真 SMTP + Docker MailHog 流程，不是固定验证码。
- 接受路径已收敛：当前 Vue 前端 + Gateway 是主页面验收路径，legacy 用于传统 Java Web 证据和兜底回归。

本项目当前短板：

- 已完成一轮浏览器人工视觉巡检，但最终课堂演示前仍应按干净数据再复检 390px、768px 和后台移动抽屉。
- 关键截图要确认与当前 UI、图片资源和文档一致。
- 最终提交包命名仍需使用本人学号姓名，不能沿用任何参考资料中的 `20236918...` 文件名。

## 前端页面横向对比

本节只比较前端路由和页面组件，不评价后端是否实际联通。扫描范围为 10 位同学的 `src/router`、`src/views` 和后台菜单文件。

### 同学前端结构摘要

| 同学/目录 | 前端形态 | 用户端页面特征 | 后台页面特征 | 相对本项目的主要差异 |
| --- | --- | --- | --- | --- |
| 董耕赫 | `store-web` + `admin-web` 双前端 | 首页、商品、活动、详情、购物车、地址、结算、订单、收藏、资料编辑、安全、反馈、通知 | 看板、商品新建/编辑独立页、订单、用户、评价、轮播、促销、优惠券、公告、反馈、角色、导入导出 | 商品维护拆成新建/编辑页面，后台导入导出入口更独立；本项目后台用抽屉/表格内操作，页面数量更少。 |
| 董新畅 | `mall-user-web` + `mall-admin-web` 双前端 | 商品、收藏、购物车、订单确认/支付、个人、地址、优惠券、客服、反馈 | 看板、用户、商品、订单、营销、反馈、客服、系统、权限、403 | 后台把营销、系统管理合并成较粗页面；本项目把轮播、公告、促销、反馈、客服拆得更细。 |
| 代宇霆 | `mall-user-web` + `mall-admin-web` 双前端 | 通知详情、促销、调研、消息中心、我的评价、评价创建、支付结果等页面较多 | 订单详情、调研管理、客服、角色、商品导入导出、统计导出 | 特色页最丰富，尤其调研、消息、统计导出；本项目更偏指导书评分点稳定覆盖。 |
| 周梦玲 | `client` + `admin` 双前端 | 秒杀专区、账户中心、商品、购物车、结算、订单、收藏、优惠券、反馈、我的咨询 | 搜索热词统计、聊天工作台、秒杀活动、管理员、轮播、公告、优惠券 | WebSocket/聊天和搜索词统计更突出；本项目客服咨询较稳但没有单独搜索热词统计页。 |
| 李博韬 | `hmall-web` + `hmall-admin` 双前端 | 分类、搜索、商品详情、通知、购物车、订单确认、订单、收藏、地址、资料、反馈、优惠券、闪购、客服 | 商品新增/编辑、分类、订单详情、评价、轮播、通知、反馈 | 页面命名偏 hmall 模板，商品新增/编辑和订单详情拆分清楚；本项目更贴合课程报告和验收矩阵。 |
| 杨浩伟 | 单个 `frontend` 聚合用户端和 `/admin` 后台 | 商品、订单、地址、收藏、反馈、聊天、新闻 | `/admin` 下看板、商品、分类、订单、用户、轮播、公告、反馈、促销、评价、设置 | 单应用聚合更省部署入口，但用户和后台边界不如本项目双前端清晰。 |
| 李旋 | `shop-web` + `admin-web` 双前端 | 与本项目页面形态高度接近，另有活动列表 | 后台页面也接近本项目，含权限、客服、轮播、促销、管理员 | 页面覆盖与本项目最相似；本项目需要避免提交包和证据材料上的同质化风险，强调真实验证和文档矩阵。 |
| 李伟翔 | `ecommerce-front` + `ecommerce-admin`，且 front 内也带 `/admin` | 用户端含分类、商品、购物车、下单、支付、订单、地址、收藏、优惠券、聊天 | 独立 admin 含优惠券、秒杀、操作日志、聊天；front 内也有后台路由 | 管理能力很全，但双处后台路由可能让演示入口变复杂；本项目入口更单一：用户端 18095、后台 18082。 |
| 郭振顺 | 单个 `web-front` 聚合用户端和 `/admin` 后台 | 促销、购物历史、我的中心嵌套路由、联系页面 | 管理员管理、商品新建/编辑、订单详情、消息、轮播、公告 | “我的中心”子路由和浏览历史值得借鉴；本项目个人中心拆成独立地址/收藏/优惠券页面，更容易截图说明。 |
| 张臻 | `client` + `admin` 双前端 | 商品、购物车、订单确认、支付、消息、收藏、优惠券、反馈 | 看板、商品、分类、订单、用户、轮播、公告、优惠券、秒杀、消息、反馈、管理员 | 结构简洁、命名清楚，支付页独立；本项目多了客服/公告/活动/安全等更细入口。 |

### 页面级差异和最佳实践

| 页面类型 | 本项目现状 | 同学项目中看到的差异 | 最佳实践 |
| --- | --- | --- | --- |
| 首页 | `shop-web /`，含搜索、分类、轮播、商品推荐、优惠券和活动入口 | 周梦玲、李伟翔、张臻更强调秒杀/促销入口；郭振顺有浏览历史；部分项目首页只是商品列表入口 | 首页首屏应同时出现搜索、分类、活动/优惠、商品卡片，保证老师不用点多层菜单就能识别电商系统。 |
| 登录/注册/找回密码 | 用户端和后台分离，用户端支持邮箱验证码找回；后台独立登录 | 多数项目都有三页；有的项目 README 或实现倾向固定验证码 | 登录页要明确区分用户和管理员入口，验证码应走真实邮件或可验证日志，不要在界面写死演示码。 |
| 商品列表/分类/搜索 | `/category/:id`、`/search` 分离，支持关键词、分类、排序和图片兜底 | 张臻、李伟翔、郭振顺统一用 `/products`；李博韬同时有 `/category` 和 `/search` | 商品列表要把筛选条件同步到 URL 或查询参数，刷新后状态不丢，空结果和图片失败要有兜底。 |
| 商品详情 | `/product/:id`，展示图片、价格、库存、参数、规格、评价、加购 | 代宇霆有评价创建入口；张臻支付/订单链路更独立 | 详情页必须把“规格选择、库存、评价、加购/购买”放在同一业务上下文中，避免只是静态商品介绍。 |
| 购物车 | `/cart`，支持数量、删除、勾选、结算 | 各项目基本都有；差异主要在移动端卡片化和空购物车引导 | 购物车应即时重算小计/总价，并提供空状态继续选购入口；移动端不要照搬宽表格。 |
| 结算/订单确认 | `/checkout`，展示地址、商品、优惠券、金额 | 张臻用 `/orders/confirm`，董新畅用 `order/confirm`；命名不同但功能相同 | 结算页要按“地址、商品、优惠、金额、提交”顺序组织，提交前能看清价格构成。 |
| 支付/支付结果 | `/payment`，模拟支付后进入订单状态链 | 代宇霆有 `pay-result`，张臻有 `/payments/:orderNo`，李伟翔有 `/order/pay/:orderNo` | 支付页应绑定订单号，支付成功后给出明确结果页或状态跳转，便于截图证明订单状态变化。 |
| 订单列表/订单详情 | `/orders`、`/orders/:id`，支持状态 Tab 和订单操作 | 多数项目都有详情；郭振顺和张臻以订单号路由更贴近业务 | 订单列表要按状态筛选，详情页要展示商品、地址、金额、物流/支付状态和可执行操作。 |
| 个人中心 | `/profile`、`/address`、`/favorites`、`/coupons` 独立页面 | 郭振顺用 `/me` 嵌套路由，董耕赫有资料编辑和安全页 | 个人中心适合保留一个总入口，再把地址、收藏、优惠券、安全设置拆成可截图的独立页面。 |
| 地址管理 | `/address`，与结算页联动 | 各项目基本覆盖，路径命名有 `/addresses`、`/user/address`、`/me/addresses` | 地址页要支持默认地址和增删改，结算页必须能读取同一份地址数据。 |
| 收藏 | `/favorites` | 各项目覆盖稳定，差异不大 | 收藏按钮、收藏列表、取消收藏必须状态一致，最好从详情页和列表页都可触发。 |
| 优惠券/促销/秒杀 | `/coupons`、`/seckill`，后台 `/promotions` | 周梦玲、张臻、李伟翔有更显眼的秒杀/活动后台；代宇霆有促销页 | 营销页要和下单金额联动，否则只是展示页；后台应能启停或配置活动。 |
| 公告/消息/通知 | `/notices`、`/activities`，后台 `/notices` | 代宇霆有通知详情和消息中心，张臻有用户消息，李伟翔有操作日志 | 公告列表最好有详情页；消息/通知应区分系统公告、订单消息和客服回复。 |
| 反馈/客服 | `/feedback`、`/service`，后台 `/feedbacks`、`/cs` | 周梦玲和李伟翔有聊天工作台；郭振顺有联系和消息页 | 客服/反馈闭环应体现“用户提交、后台处理、用户查看”，实时聊天是加分项但不是必须。 |
| 后台登录和布局 | 管理端独立 `admin-web`，`/login`、`/dashboard`，移动端抽屉菜单 | 杨浩伟、郭振顺单前端聚合后台；李伟翔 front/admin 两处后台路由 | 后台最好独立入口并有路由守卫，避免用户端和后台菜单混在一起影响演示路径。 |
| 数据看板 | `/dashboard`，统计卡片和图表 | 多数项目都有；代宇霆/周梦玲/张臻统计维度更多 | 看板首屏要有用户数、订单数、销售额、订单状态和趋势图，数据为空时仍要有稳定布局。 |
| 后台用户管理 | `/users`，列表、搜索、启停、详情 | 多数项目覆盖；郭振顺另有管理员管理 | 用户管理要把普通用户和管理员权限分开，避免同一表格里混淆角色。 |
| 后台商品/分类/评价 | `/products`、`/categories`、`/reviews` | 董耕赫、李博韬、郭振顺把商品新增/编辑拆成独立路由；本项目用表格和抽屉 | 商品管理要支持搜索、分类、上下架、库存、图片上传；独立编辑页适合复杂表单，抽屉适合课堂快速演示。 |
| 后台订单管理 | `/orders` | 多数项目有订单详情；郭振顺、张臻用订单号路由 | 后台订单页要能筛选状态、查看详情、发货/取消/退款，并与用户订单状态同步。 |
| 后台内容运营 | `/banners`、`/notices`、`/promotions` | 周梦玲有搜索热词统计，李伟翔有日志，代宇霆有统计导出 | 运营页不必很多，但轮播、公告、优惠券至少要能新增、启停、排序，并能在用户端看到效果。 |
| 后台权限/管理员 | `/permission-manage`，`/roles` 和 `/admins` 重定向 | 董耕赫有 roles，代宇霆有 roles，张臻有 admin-users，郭振顺有 admins | 权限页要和菜单显示、接口 403 结果对应；否则只是一张管理员表。 |
| 响应式页面 | 已记录 390px、768px 巡检，后台有移动抽屉 | 李旋项目有类似移动抽屉；多数同学不一定显式记录 | 响应式不是只缩小首页，至少要检查首页、商品详情、购物车、结算、订单和后台菜单。 |

### 对本项目的取舍结论

可以借鉴但不建议临近提交大改的点：

- 商品新增/编辑独立页：董耕赫、李博韬、郭振顺都有，适合复杂商品表单；本项目当前抽屉式管理更快演示，保留即可。
- 支付结果页：代宇霆、张臻的支付结果路由更清楚；本项目已有 `/payment` 和订单状态链，文档里强调模拟支付结果即可。
- 搜索热词统计、浏览历史、操作日志：周梦玲、郭振顺、李伟翔有亮点，但不是指导书核心项，不建议现在追加。
- 消息中心/实时聊天：代宇霆、周梦玲、李伟翔更丰富；本项目已有客服咨询和反馈闭环，足够覆盖课程评分。

最应保持的本项目做法：

- 双 Vue 前端独立部署，用户端 `18095`、管理端 `18082`，入口清楚。
- 页面覆盖与指导书评分点一一对应，不为了页面数量增加演示复杂度。
- 图片本地化和 placeholder 兜底，避免课堂现场依赖外网图片。
- 文档、截图、自动化 smoke 和人工巡检记录放在 `docs/course/acceptance`，能解释“页面能看、接口能通、评分点能对上”。

## 提交规范风险排名

高风险：

- 李旋：zip 约 532M，含 `.git`、`.npm-cache`、数据库 `.ibd/.sdi` 文件，远超 50M。
- 李博韬：zip 约 215M，含大量缓存/生成物，远超 50M。

中风险：

- 李伟翔：zip 约 12M 未超 50M，但包含 `.git`、`.idea`、`.claude`。
- 杨浩伟：zip 约 0.5M 很小，但 zip 清单显示有少量 `.git` 相关条目。

低风险：

- 董耕赫、董新畅、张臻、周梦玲、郭振顺、代宇霆的 zip 大小基本可控或未发现严重缓存目录；其中郭振顺 24M 接近但仍低于 50M。

## 结论

按“降低我们大作业评分风险”的目标看，不建议再照搬任何同学的大功能。我们的更优路径是继续补齐验收证据：浏览器人工巡检、截图新鲜度、文档迁移清理、最终提交包命名与个人信息确认。

如果只看功能野心，周梦玲、代宇霆、李伟翔、张臻更丰富；如果看提交可控性和评分稳定性，本项目当前路线更稳。多个同学项目虽然功能写得多，但存在依赖复杂、README 与源码状态不一致、提交包超标或不可复现的问题，这些都是答辩和评分时的实际风险。

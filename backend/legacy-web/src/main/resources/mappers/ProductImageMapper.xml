<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ecommerce.mapper.ProductImageMapper">
    <select id="findByProductId" resultType="com.example.ecommerce.model.ProductImage">
        SELECT id, product_id AS productId, url, sort_order AS sortOrder
        FROM product_image WHERE product_id = #{productId} AND deleted = 0 ORDER BY sort_order ASC, id ASC
    </select>
</mapper>
